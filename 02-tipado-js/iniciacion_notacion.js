//notaciones 
// ; -> Delimitar el final de una linea 
const b = 4
const a = 4; //-> si gusta se puede poner o no, como buena practica se puede poner

// . --> Se utiliza en los objetos pata acceder a los atributos 
//movil.anchura 

// [] --> Lista, arreglos o arrays
const ar = [1, 2, 3, 4];
console.log(ar[2]);

// () --> Funciones
function suma (a, b){
   // Se escribe la funcion
}
// {} --> llaves para objetos, funciones y estructuras de control
const movil = {
   anchura: 5,
   altura: 10,
}
if (true) {
   // Todo lo que haya en las llaves 

   const constate2 = 'hola';
}